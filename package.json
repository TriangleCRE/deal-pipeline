{
  "name": "deal-pipeline",
  "version": "1.0.0",
  "private": true,
  "description": "Triangle Investment Group deal pipeline — property cards stored as JSON documents in Postgres.",
  "type": "module",
  "scripts": {
    "db:setup": "node scripts/setup-db.mjs",
    "db:seed": "node scripts/seed.mjs",
    "gen:prompt": "node scripts/generate-prompt.mjs",
    "dev": "node scripts/dev-server.mjs"
  },
  "dependencies": {
    "pg": "^8.13.1",
    "zod": "^3.24.1"
  }
}
